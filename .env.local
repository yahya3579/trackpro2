# Database configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=trackpro_db

# API base URL (for frontend to use)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# JWT secret
JWT_SECRET=trackpro-secret-key

# SMTP settings for email (update for production)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ninjab330@gmail.com
SMTP_PASS=dylj bvbr rbxn fmno